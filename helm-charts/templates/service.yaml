apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-rate-limit.fullname" . }}
  labels:
    {{- include "docker-rate-limit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "docker-rate-limit.selectorLabels" . | nindent 4 }}
